export const codeSnippets = {
  'Bubble Sort': {
    C: `void bubbleSort(int arr[], int n) {\n    for (int i = 0; i < n - 1; i++)\n        for (int j = 0; j < n - i - 1; j++)\n            if (arr[j] > arr[j + 1])\n                swap(&arr[j], &arr[j + 1]);\n}`,
    'C++': `void bubbleSort(vector<int>& arr) {\n    int n = arr.size();\n    for (int i = 0; i < n - 1; i++) {\n        for (int j = 0; j < n - i - 1; j++) {\n            if (arr[j] > arr[j + 1]) swap(arr[j], arr[j + 1]);\n        }\n    }\n}`,
    Java: `void bubbleSort(int arr[]) {\n    int n = arr.length;\n    for (int i = 0; i < n - 1; i++)\n        for (int j = 0; j < n - i - 1; j++)\n            if (arr[j] > arr[j + 1]) {\n                int temp = arr[j]; arr[j] = arr[j + 1]; arr[j + 1] = temp;\n            }\n}`,
    Python: `def bubble_sort(arr):\n    n = len(arr)\n    for i in range(n - 1):\n        for j in range(n - i - 1):\n            if arr[j] > arr[j + 1]:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]`,
    Javascript: `function bubbleSort(arr) {\n    const n = arr.length;\n    for (let i = 0; i < n - 1; i++) {\n        for (let j = 0; j < n - i - 1; j++) {\n            if (arr[j] > arr[j + 1]) [arr[j], arr[j+1]] = [arr[j+1], arr[j]];\n        }\n    }\n}`
  },
  'Selection Sort': {
    C: `void selectionSort(int arr[], int n) {\n    for (int i = 0; i < n-1; i++) {\n        int min_idx = i;\n        for (int j = i+1; j < n; j++)\n          if (arr[j] < arr[min_idx]) min_idx = j;\n        swap(&arr[min_idx], &arr[i]);\n    }\n}`,
    'C++': `void selectionSort(vector<int>& arr) {\n    for (int i = 0; i < arr.size()-1; i++) {\n        int min_idx = i;\n        for (int j = i+1; j < arr.size(); j++)\n          if (arr[j] < arr[min_idx]) min_idx = j;\n        swap(arr[min_idx], arr[i]);\n    }\n}`,
    Java: `void selectionSort(int arr[]) {\n    for (int i = 0; i < arr.length-1; i++) {\n        int min_idx = i;\n        for (int j = i+1; j < arr.length; j++)\n            if (arr[j] < arr[min_idx]) min_idx = j;\n        int temp = arr[min_idx]; arr[min_idx] = arr[i]; arr[i] = temp;\n    }\n}`,
    Python: `def selection_sort(arr):\n    for i in range(len(arr)):\n        min_idx = i\n        for j in range(i+1, len(arr)):\n            if arr[min_idx] > arr[j]: min_idx = j\n        arr[i], arr[min_idx] = arr[min_idx], arr[i]`,
    Javascript: `function selectionSort(arr) {\n    for(let i=0; i<arr.length; i++) {\n        let min = i;\n        for(let j=i+1; j<arr.length; j++) {\n            if(arr[j] < arr[min]) min = j;\n        }\n        if(min !== i) [arr[i], arr[min]] = [arr[min], arr[i]];\n    }\n}`
  },
  'Insertion Sort': {
    C: `void insertionSort(int arr[], int n) {\n    for (int i = 1; i < n; i++) {\n        int key = arr[i];\n        int j = i - 1;\n        while (j >= 0 && arr[j] > key) { arr[j + 1] = arr[j]; j = j - 1; }\n        arr[j + 1] = key;\n    }\n}`,
    'C++': `void insertionSort(vector<int>& arr) {\n    for (int i = 1; i < arr.size(); i++) {\n        int key = arr[i], j = i - 1;\n        while (j >= 0 && arr[j] > key) { arr[j + 1] = arr[j]; j--; }\n        arr[j + 1] = key;\n    }\n}`,
    Java: `void insertionSort(int arr[]) {\n    for (int i = 1; i < arr.length; ++i) {\n        int key = arr[i], j = i - 1;\n        while (j >= 0 && arr[j] > key) { arr[j + 1] = arr[j]; j--; }\n        arr[j + 1] = key;\n    }\n}`,
    Python: `def insertion_sort(arr):\n    for i in range(1, len(arr)):\n        key = arr[i]\n        j = i-1\n        while j >= 0 and key < arr[j] : arr[j + 1] = arr[j]; j -= 1\n        arr[j + 1] = key`,
    Javascript: `function insertionSort(arr) {\n    for (let i = 1; i < arr.length; i++) {\n        let key = arr[i], j = i - 1;\n        while (j >= 0 && arr[j] > key) { arr[j + 1] = arr[j]; j = j - 1; }\n        arr[j + 1] = key;\n    }\n}`
  },
  'Merge Sort': {
    C: `void merge(int arr[], int l, int m, int r) {\n    int i, j, k;\n    int n1 = m - l + 1, n2 = r - m;\n    int L[n1], R[n2];\n    for (i = 0; i < n1; i++) L[i] = arr[l + i];\n    for (j = 0; j < n2; j++) R[j] = arr[m + 1 + j];\n    i = 0; j = 0; k = l;\n    while (i < n1 && j < n2) {\n        if (L[i] <= R[j]) { arr[k] = L[i]; i++; }\n        else { arr[k] = R[j]; j++; }\n        k++;\n    }\n    while (i < n1) { arr[k] = L[i]; i++; k++; }\n    while (j < n2) { arr[k] = R[j]; j++; k++; }\n}\nvoid mergeSort(int arr[], int l, int r) {\n    if (l < r) {\n        int m = l + (r - l) / 2;\n        mergeSort(arr, l, m);\n        mergeSort(arr, m + 1, r);\n        merge(arr, l, m, r);\n    }\n}`,
    'C++': `void merge(vector<int>& arr, int l, int m, int r) {\n    int n1 = m - l + 1, n2 = r - m;\n    vector<int> L(n1), R(n2);\n    for(int i = 0; i < n1; i++) L[i] = arr[l + i];\n    for(int j = 0; j < n2; j++) R[j] = arr[m + 1 + j];\n    int i = 0, j = 0, k = l;\n    while(i < n1 && j < n2) {\n        if(L[i] <= R[j]) arr[k++] = L[i++];\n        else arr[k++] = R[j++];\n    }\n    while(i < n1) arr[k++] = L[i++];\n    while(j < n2) arr[k++] = R[j++];\n}\nvoid mergeSort(vector<int>& arr, int l, int r) {\n    if(l >= r) return;\n    int m = l + (r - l) / 2;\n    mergeSort(arr, l, m);\n    mergeSort(arr, m + 1, r);\n    merge(arr, l, m, r);\n}`,
    Java: `void merge(int arr[], int l, int m, int r) {\n    int n1 = m - l + 1, n2 = r - m;\n    int L[] = new int[n1], R[] = new int[n2];\n    for (int i = 0; i < n1; ++i) L[i] = arr[l + i];\n    for (int j = 0; j < n2; ++j) R[j] = arr[m + 1 + j];\n    int i = 0, j = 0, k = l;\n    while (i < n1 && j < n2) {\n        if (L[i] <= R[j]) { arr[k] = L[i]; i++; }\n        else { arr[k] = R[j]; j++; }\n        k++;\n    }\n    while (i < n1) { arr[k] = L[i]; i++; k++; }\n    while (j < n2) { arr[k] = R[j]; j++; k++; }\n}\nvoid mergeSort(int arr[], int l, int r) {\n    if (l < r) {\n        int m = l + (r - l) / 2;\n        mergeSort(arr, l, m);\n        mergeSort(arr, m + 1, r);\n        merge(arr, l, m, r);\n    }\n}`,
    Python: `def merge_sort(arr):\n    if len(arr) > 1:\n        mid = len(arr) // 2\n        L, R = arr[:mid], arr[mid:]\n        merge_sort(L)\n        merge_sort(R)\n        i = j = k = 0\n        while i < len(L) and j < len(R):\n            if L[i] < R[j]: arr[k] = L[i]; i += 1\n            else: arr[k] = R[j]; j += 1\n            k += 1\n        while i < len(L): arr[k] = L[i]; i += 1; k += 1\n        while j < len(R): arr[k] = R[j]; j += 1; k += 1`,
    Javascript: `function mergeSort(arr) {\n    if (arr.length <= 1) return arr;\n    const mid = Math.floor(arr.length / 2);\n    const L = mergeSort(arr.slice(0, mid));\n    const R = mergeSort(arr.slice(mid));\n    let i = 0, j = 0, k = 0;\n    while (i < L.length && j < R.length) {\n        if (L[i] < R[j]) arr[k++] = L[i++];\n        else arr[k++] = R[j++];\n    }\n    while (i < L.length) arr[k++] = L[i++];\n    while (j < R.length) arr[k++] = R[j++];\n    return arr;\n}`
  },
  'Quick Sort': {
    C: `int partition (int arr[], int low, int high) {\n    int pivot = arr[high], i = (low - 1);\n    for (int j = low; j <= high - 1; j++) {\n        if (arr[j] < pivot) {\n            i++;\n            swap(&arr[i], &arr[j]);\n        }\n    }\n    swap(&arr[i + 1], &arr[high]);\n    return (i + 1);\n}\nvoid quickSort(int arr[], int low, int high) {\n    if (low < high) {\n        int pi = partition(arr, low, high);\n        quickSort(arr, low, pi - 1);\n        quickSort(arr, pi + 1, high);\n    }\n}`,
    'C++': `int partition(vector<int>& arr, int low, int high) {\n    int pivot = arr[high], i = (low - 1);\n    for (int j = low; j <= high - 1; j++) {\n        if (arr[j] < pivot) {\n            i++;\n            swap(arr[i], arr[j]);\n        }\n    }\n    swap(arr[i + 1], arr[high]);\n    return (i + 1);\n}\nvoid quickSort(vector<int>& arr, int low, int high) {\n    if (low < high) {\n        int pi = partition(arr, low, high);\n        quickSort(arr, low, pi - 1);\n        quickSort(arr, pi + 1, high);\n    }\n}`,
    Java: `int partition(int arr[], int low, int high) {\n    int pivot = arr[high], i = (low - 1);\n    for (int j = low; j <= high - 1; j++) {\n        if (arr[j] < pivot) {\n            i++;\n            int temp = arr[i]; arr[i] = arr[j]; arr[j] = temp;\n        }\n    }\n    int temp = arr[i + 1]; arr[i + 1] = arr[high]; arr[high] = temp;\n    return (i + 1);\n}\nvoid quickSort(int arr[], int low, int high) {\n    if (low < high) {\n        int pi = partition(arr, low, high);\n        quickSort(arr, low, pi - 1);\n        quickSort(arr, pi + 1, high);\n    }\n}`,
    Python: `def partition(arr, low, high):\n    pivot, i = arr[high], low - 1\n    for j in range(low, high):\n        if arr[j] <= pivot:\n            i = i + 1\n            arr[i], arr[j] = arr[j], arr[i]\n    arr[i + 1], arr[high] = arr[high], arr[i + 1]\n    return i + 1\n\ndef quick_sort(arr, low, high):\n    if low < high:\n        pi = partition(arr, low, high)\n        quick_sort(arr, low, pi - 1)\n        quick_sort(arr, pi + 1, high)`,
    Javascript: `function partition(arr, low, high) {\n    let pivot = arr[high], i = (low - 1);\n    for (let j = low; j <= high - 1; j++) {\n        if (arr[j] < pivot) {\n            i++;\n            [arr[i], arr[j]] = [arr[j], arr[i]];\n        }\n    }\n    [arr[i + 1], arr[high]] = [arr[high], arr[i + 1]];\n    return (i + 1);\n}\nfunction quickSort(arr, low, high) {\n    if (low < high) {\n        let pi = partition(arr, low, high);\n        quickSort(arr, low, pi - 1);\n        quickSort(arr, pi + 1, high);\n    }\n}`
  },
  'BFS': {
    C: `void bfs(int adj[][MAX], int v, int start) {\n    int queue[MAX], front = 0, rear = 0;\n    int visited[MAX] = {0};\n    visited[start] = 1;\n    queue[rear++] = start;\n    while(front < rear) {\n        int node = queue[front++];\n        printf("%d ", node);\n        for(int i = 0; i < v; i++) {\n            if(adj[node][i] == 1 && !visited[i]) {\n                visited[i] = 1;\n                queue[rear++] = i;\n            }\n        }\n    }\n}`,
    'C++': `void bfs(vector<vector<int>>& adj, int start) {\n    int v = adj.size();\n    vector<bool> visited(v, false);\n    queue<int> q;\n    visited[start] = true;\n    q.push(start);\n    while(!q.empty()) {\n        int node = q.front();\n        q.pop();\n        cout << node << " ";\n        for(int neighbor : adj[node]) {\n            if(!visited[neighbor]) {\n                visited[neighbor] = true;\n                q.push(neighbor);\n            }\n        }\n    }\n}`,
    Java: `void bfs(ArrayList<ArrayList<Integer>> adj, int start) {\n    int v = adj.size();\n    boolean visited[] = new boolean[v];\n    Queue<Integer> q = new LinkedList<>();\n    visited[start] = true;\n    q.add(start);\n    while(!q.isEmpty()) {\n        int node = q.poll();\n        System.out.print(node + " ");\n        for(int neighbor : adj.get(node)) {\n            if(!visited[neighbor]) {\n                visited[neighbor] = true;\n                q.add(neighbor);\n            }\n        }\n    }\n}`,
    Python: `def bfs(adj, start):\n    visited = [False] * len(adj)\n    queue = [start]\n    visited[start] = True\n    while queue:\n        node = queue.pop(0)\n        print(node, end=" ")\n        for neighbor in adj[node]:\n            if not visited[neighbor]:\n                visited[neighbor] = True\n                queue.append(neighbor)`,
    Javascript: `function bfs(adj, start) {\n    let visited = new Array(adj.length).fill(false);\n    let queue = [start];\n    visited[start] = true;\n    while(queue.length > 0) {\n        let node = queue.shift();\n        console.log(node);\n        for(let neighbor of adj[node]) {\n            if(!visited[neighbor]) {\n                visited[neighbor] = true;\n                queue.push(neighbor);\n            }\n        }\n    }\n}`
  },
  'DFS': {
    C: `void dfs(int adj[][MAX], int v, int node, int visited[]) {\n    visited[node] = 1;\n    printf("%d ", node);\n    for (int i = 0; i < v; i++) {\n        if (adj[node][i] == 1 && !visited[i]) {\n            dfs(adj, v, i, visited);\n        }\n    }\n}`,
    'C++': `void dfs(vector<vector<int>>& adj, int node, vector<bool>& visited) {\n    visited[node] = true;\n    cout << node << " ";\n    for (int neighbor : adj[node]) {\n        if (!visited[neighbor]) {\n            dfs(adj, neighbor, visited);\n        }\n    }\n}`,
    Java: `void dfs(ArrayList<ArrayList<Integer>> adj, int node, boolean visited[]) {\n    visited[node] = true;\n    System.out.print(node + " ");\n    for (int neighbor : adj.get(node)) {\n        if (!visited[neighbor]) {\n            dfs(adj, neighbor, visited);\n        }\n    }\n}`,
    Python: `def dfs(adj, node, visited):\n    visited[node] = True\n    print(node, end=" ")\n    for neighbor in adj[node]:\n        if not visited[neighbor]:\n            dfs(adj, neighbor, visited)`,
    Javascript: `function dfs(adj, node, visited) {\n    visited[node] = true;\n    console.log(node);\n    for(let neighbor of adj[node]) {\n        if(!visited[neighbor]) {\n            dfs(adj, neighbor, visited);\n        }\n    }\n}`
  },
  'Dijkstra': {
    C: `void dijkstra(int graph[V][V], int src) {\n    int dist[V];\n    bool sptSet[V];\n    for (int i = 0; i < V; i++) dist[i] = INT_MAX, sptSet[i] = false;\n    dist[src] = 0;\n    for (int count = 0; count < V - 1; count++) {\n        int u = minDistance(dist, sptSet);\n        sptSet[u] = true;\n        for (int v = 0; v < V; v++)\n            if (!sptSet[v] && graph[u][v] && dist[u] != INT_MAX && dist[u] + graph[u][v] < dist[v])\n                dist[v] = dist[u] + graph[u][v];\n    }\n}`,
    'C++': `void dijkstra(vector<vector<pair<int, int>>>& adj, int src) {\n    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;\n    vector<int> dist(adj.size(), INT_MAX);\n    pq.push(make_pair(0, src));\n    dist[src] = 0;\n    while (!pq.empty()) {\n        int u = pq.top().second;\n        pq.pop();\n        for (auto x : adj[u]) {\n            int v = x.first, weight = x.second;\n            if (dist[v] > dist[u] + weight) {\n                dist[v] = dist[u] + weight;\n                pq.push(make_pair(dist[v], v));\n            }\n        }\n    }\n}`,
    Java: `void dijkstra(int graph[][], int src, int V) {\n    int dist[] = new int[V];\n    Boolean sptSet[] = new Boolean[V];\n    for (int i = 0; i < V; i++) { dist[i] = Integer.MAX_VALUE; sptSet[i] = false; }\n    dist[src] = 0;\n    for (int count = 0; count < V - 1; count++) {\n        int u = minDistance(dist, sptSet, V);\n        sptSet[u] = true;\n        for (int v = 0; v < V; v++)\n            if (!sptSet[v] && graph[u][v] != 0 && dist[u] != Integer.MAX_VALUE && dist[u] + graph[u][v] < dist[v])\n                dist[v] = dist[u] + graph[u][v];\n    }\n}`,
    Python: `def dijkstra(graph, src):\n    import heapq\n    dist = [float("inf")] * len(graph)\n    dist[src] = 0\n    pq = [(0, src)]\n    while pq:\n        d, u = heapq.heappop(pq)\n        if d > dist[u]: continue\n        for v, weight in graph[u]:\n            if dist[u] + weight < dist[v]:\n                dist[v] = dist[u] + weight\n                heapq.heappush(pq, (dist[v], v))\n    return dist`,
    Javascript: `function dijkstra(graph, src) {\n    const dist = new Array(graph.length).fill(Infinity);\n    dist[src] = 0;\n    const pq = [[0, src]];\n    while(pq.length > 0) {\n        pq.sort((a,b) => a[0] - b[0]);\n        const [d, u] = pq.shift();\n        if (d > dist[u]) continue;\n        for (let [v, weight] of graph[u]) {\n            if (dist[u] + weight < dist[v]) {\n                dist[v] = dist[u] + weight;\n                pq.push([dist[v], v]);\n            }\n        }\n    }\n    return dist;\n}`
  },
  'Topological Sort': {
    C: `void topologicalSortUtil(int v, bool visited[], int stack[], int *top, int adj[][MAX], int V) {\n    visited[v] = true;\n    for (int i = 0; i < V; i++) {\n        if (adj[v][i] && !visited[i])\n            topologicalSortUtil(i, visited, stack, top, adj, V);\n    }\n    stack[++(*top)] = v;\n}\nvoid topologicalSort(int adj[][MAX], int V) {\n    int stack[MAX], top = -1;\n    bool visited[V];\n    for (int i = 0; i < V; i++) visited[i] = false;\n    for (int i = 0; i < V; i++) {\n        if (visited[i] == false)\n            topologicalSortUtil(i, visited, stack, &top, adj, V);\n    }\n    while (top >= 0) printf("%d ", stack[top--]);\n}`,
    'C++': `void topSortUtil(int v, vector<vector<int>>& adj, vector<bool>& visited, stack<int>& Stack) {\n    visited[v] = true;\n    for (int neighbor : adj[v]) {\n        if (!visited[neighbor])\n            topSortUtil(neighbor, adj, visited, Stack);\n    }\n    Stack.push(v);\n}\nvoid topologicalSort(vector<vector<int>>& adj) {\n    stack<int> Stack;\n    vector<bool> visited(adj.size(), false);\n    for (int i = 0; i < adj.size(); i++) {\n        if (!visited[i]) topSortUtil(i, adj, visited, Stack);\n    }\n    while (!Stack.empty()) {\n        cout << Stack.top() << " ";\n        Stack.pop();\n    }\n}`,
    Java: `void topSortUtil(int v, ArrayList<ArrayList<Integer>> adj, boolean visited[], Stack<Integer> stack) {\n    visited[v] = true;\n    for (Integer neighbor : adj.get(v)) {\n        if (!visited[neighbor])\n            topSortUtil(neighbor, adj, visited, stack);\n    }\n    stack.push(v);\n}\nvoid topologicalSort(ArrayList<ArrayList<Integer>> adj, int V) {\n    Stack<Integer> stack = new Stack<Integer>();\n    boolean visited[] = new boolean[V];\n    for (int i = 0; i < V; i++) {\n        if (visited[i] == false) topSortUtil(i, adj, visited, stack);\n    }\n    while (!stack.empty()) System.out.print(stack.pop() + " ");\n}`,
    Python: `def topSortUtil(v, adj, visited, stack):\n    visited[v] = True\n    for neighbor in adj[v]:\n        if not visited[neighbor]:\n            topSortUtil(neighbor, adj, visited, stack)\n    stack.append(v)\n\ndef topologicalSort(adj, V):\n    visited = [False]*V\n    stack = []\n    for i in range(V):\n        if not visited[i]:\n            topSortUtil(i, adj, visited, stack)\n    print(stack[::-1])`,
    Javascript: `function topSortUtil(v, adj, visited, stack) {\n    visited[v] = true;\n    for (let neighbor of adj[v]) {\n        if (!visited[neighbor]) topSortUtil(neighbor, adj, visited, stack);\n    }\n    stack.push(v);\n}\nfunction topologicalSort(adj, V) {\n    let stack = [];\n    let visited = new Array(V).fill(false);\n    for (let i = 0; i < V; i++) {\n        if (!visited[i]) topSortUtil(i, adj, visited, stack);\n    }\n    console.log(stack.reverse());\n}`
  },
  'Bellman-Ford': {
    C: `void BellmanFord(struct Graph* graph, int src) {\n    int V = graph->V, E = graph->E;\n    int dist[V];\n    for (int i = 0; i < V; i++) dist[i] = INT_MAX;\n    dist[src] = 0;\n    for (int i = 1; i <= V - 1; i++) {\n        for (int j = 0; j < E; j++) {\n            int u = graph->edge[j].src, v = graph->edge[j].dest, weight = graph->edge[j].weight;\n            if (dist[u] != INT_MAX && dist[u] + weight < dist[v]) dist[v] = dist[u] + weight;\n        }\n    }\n}`,
    'C++': `void BellmanFord(vector<vector<int>>& edges, int V, int src) {\n    vector<int> dist(V, INT_MAX);\n    dist[src] = 0;\n    for (int i = 1; i <= V - 1; i++) {\n        for (auto edge : edges) {\n            int u = edge[0], v = edge[1], weight = edge[2];\n            if (dist[u] != INT_MAX && dist[u] + weight < dist[v]) dist[v] = dist[u] + weight;\n        }\n    }\n}`,
    Java: `void BellmanFord(int graph[][], int V, int E, int src) {\n    int dist[] = new int[V];\n    for (int i = 0; i < V; ++i) dist[i] = Integer.MAX_VALUE;\n    dist[src] = 0;\n    for (int i = 1; i < V; ++i) {\n        for (int j = 0; j < E; ++j) {\n            int u = graph[j][0], v = graph[j][1], weight = graph[j][2];\n            if (dist[u] != Integer.MAX_VALUE && dist[u] + weight < dist[v]) dist[v] = dist[u] + weight;\n        }\n    }\n}`,
    Python: `def bellman_ford(graph, V, src):\n    dist = [float("inf")] * V\n    dist[src] = 0\n    for _ in range(V - 1):\n        for u, v, w in graph:\n            if dist[u] != float("inf") and dist[u] + w < dist[v]:\n                dist[v] = dist[u] + w\n    return dist`,
    Javascript: `function bellmanFord(edges, V, src) {\n    let dist = new Array(V).fill(Infinity);\n    dist[src] = 0;\n    for (let i = 0; i < V - 1; i++) {\n        for (let [u, v, w] of edges) {\n            if (dist[u] !== Infinity && dist[u] + w < dist[v]) dist[v] = dist[u] + w;\n        }\n    }\n    return dist;\n}`
  },
  'A* Search': {
    C: `// A* Search C implementation requires a priority queue and heuristics.\n// Logic uses f(n) = g(n) + h(n) where h is estimated distance.`,
    'C++': `// A* Search C++ uses priority_queue<pair<int, int>>pq;\n// f(n) = g(n) + h(n)`,
    Java: `// A* Search Java uses PriorityQueue<Node>\n// Explore with f(n) = g(n) + h(n)`,
    Python: `def a_star(graph, start, goal, h):\n    open_set = set([start])\n    g_score = {start: 0}\n    f_score = {start: h(start)}\n    while open_set:\n        current = min(open_set, key=lambda x: f_score.get(x, float('inf')))\n        if current == goal: return True\n        open_set.remove(current)\n        for neighbor, weight in graph[current]:\n            tentative_g = g_score[current] + weight\n            if tentative_g < g_score.get(neighbor, float('inf')):\n                g_score[neighbor] = tentative_g\n                f_score[neighbor] = tentative_g + h(neighbor)\n                open_set.add(neighbor)`,
    Javascript: `// A* Javascript Implementation\n// Computes f(n) = g(n) + h(n) dynamically.`
  },
  'Tarjan algorithm': {
    C: `void dfs(int u, int disc[], int low[], bool stackMember[], int st[], int* head, int adj[][MAX], int V) {\n    static int time = 0;\n    disc[u] = low[u] = ++time;\n    st[++(*head)] = u;\n    stackMember[u] = true;\n    for (int v = 0; v < V; v++) {\n        if (adj[u][v]) {\n            if (disc[v] == -1) {\n                dfs(v, disc, low, stackMember, st, head, adj, V);\n                low[u] = min(low[u], low[v]);\n            } else if (stackMember[v]) {\n                low[u] = min(low[u], disc[v]);\n            }\n        }\n    }\n    if (low[u] == disc[u]) {\n        while (st[*head] != u) {\n            int v = st[(*head)--];\n            printf("%d ", v);\n            stackMember[v] = false;\n        }\n        int v = st[(*head)--];\n        printf("%d\\n", v);\n        stackMember[v] = false;\n    }\n}`,
    'C++': `void tarjanDFS(int u, vector<int>& disc, vector<int>& low, stack<int>& st, vector<bool>& inStack, vector<vector<int>>& adj, int& time) {\n    disc[u] = low[u] = ++time;\n    st.push(u);\n    inStack[u] = true;\n    for (int v : adj[u]) {\n        if (disc[v] == -1) {\n            tarjanDFS(v, disc, low, st, inStack, adj, time);\n            low[u] = min(low[u], low[v]);\n        } else if (inStack[v]) {\n            low[u] = min(low[u], disc[v]);\n        }\n    }\n    if (low[u] == disc[u]) {\n        while (true) {\n            int v = st.top(); st.pop();\n            inStack[v] = false;\n            cout << v << " ";\n            if (u == v) break;\n        }\n        cout << "\\n";\n    }\n}`,
    Java: `void tarjanDFS(int u, int[] disc, int[] low, Stack<Integer> st, boolean[] inStack, List<List<Integer>> adj, int time) {\n    disc[u] = low[u] = ++time;\n    st.push(u);\n    inStack[u] = true;\n    for (int v : adj.get(u)) {\n        if (disc[v] == -1) {\n            tarjanDFS(v, disc, low, st, inStack, adj, time);\n            low[u] = Math.min(low[u], low[v]);\n        } else if (inStack[v]) {\n            low[u] = Math.min(low[u], disc[v]);\n        }\n    }\n    if (low[u] == disc[u]) {\n        while (true) {\n            int v = st.pop();\n            inStack[v] = false;\n            System.out.print(v + " ");\n            if (u == v) break;\n        }\n        System.out.println();\n    }\n}`,
    Python: `def tarjan(adj):\n    n = len(adj)\n    disc = [-1] * n\n    low = [-1] * n\n    in_stack = [False] * n\n    stack = []\n    time = 0\n    sccs = []\n    \n    def dfs(u):\n        nonlocal time\n        disc[u] = low[u] = time\n        time += 1\n        stack.append(u)\n        in_stack[u] = True\n        for v in adj[u]:\n            if disc[v] == -1:\n                dfs(v)\n                low[u] = min(low[u], low[v])\n            elif in_stack[v]:\n                low[u] = min(low[u], disc[v])\n        if low[u] == disc[u]:\n            scc = []\n            while True:\n                v = stack.pop()\n                in_stack[v] = False\n                scc.append(v)\n                if u == v: break\n            sccs.append(scc)\n            \n    for i in range(n):\n        if disc[i] == -1: dfs(i)\n    return sccs`,
    Javascript: `function tarjan(adj) {\n    const n = adj.length;\n    const disc = Array(n).fill(-1);\n    const low = Array(n).fill(-1);\n    const inStack = Array(n).fill(false);\n    const stack = [];\n    let time = 0;\n    const sccs = [];\n    \n    function dfs(u) {\n        disc[u] = low[u] = time++;\n        stack.push(u);\n        inStack[u] = true;\n        for (let v of adj[u]) {\n            if (disc[v] === -1) {\n                dfs(v);\n                low[u] = Math.min(low[u], low[v]);\n            } else if (inStack[v]) {\n                low[u] = Math.min(low[u], disc[v]);\n            }\n        }\n        if (low[u] === disc[u]) {\n            const scc = [];\n            while (true) {\n                const v = stack.pop();\n                inStack[v] = false;\n                scc.push(v);\n                if (u === v) break;\n            }\n            sccs.push(scc);\n        }\n    }\n    for (let i = 0; i < n; i++) {\n        if (disc[i] === -1) dfs(i);\n    }\n    return sccs;\n}`
  },
  'Knapsack 0/1': {
    C: `int knapSack(int W, int wt[], int val[], int n) {\n    int i, w, K[n + 1][W + 1];\n    for (i = 0; i <= n; i++) {\n        for (w = 0; w <= W; w++) {\n            if (i == 0 || w == 0) K[i][w] = 0;\n            else if (wt[i - 1] <= w) K[i][w] = max(val[i - 1] + K[i - 1][w - wt[i - 1]], K[i - 1][w]);\n            else K[i][w] = K[i - 1][w];\n        }\n    }\n    return K[n][W];\n}`,
    'C++': `int knapSack(int W, vector<int>& wt, vector<int>& val, int n) {\n    vector<vector<int>> K(n + 1, vector<int>(W + 1));\n    for (int i = 0; i <= n; i++) {\n        for (int w = 0; w <= W; w++) {\n            if (i == 0 || w == 0) K[i][w] = 0;\n            else if (wt[i - 1] <= w) K[i][w] = max(val[i - 1] + K[i - 1][w - wt[i - 1]], K[i - 1][w]);\n            else K[i][w] = K[i - 1][w];\n        }\n    }\n    return K[n][W];\n}`,
    Java: `int knapSack(int W, int wt[], int val[], int n) {\n    int K[][] = new int[n + 1][W + 1];\n    for (int i = 0; i <= n; i++) {\n        for (int w = 0; w <= W; w++) {\n            if (i == 0 || w == 0) K[i][w] = 0;\n            else if (wt[i - 1] <= w) K[i][w] = Math.max(val[i - 1] + K[i - 1][w - wt[i - 1]], K[i - 1][w]);\n            else K[i][w] = K[i - 1][w];\n        }\n    }\n    return K[n][W];\n}`,
    Python: `def knapSack(W, wt, val, n):\n    K = [[0 for x in range(W + 1)] for x in range(n + 1)]\n    for i in range(n + 1):\n        for w in range(W + 1):\n            if i == 0 or w == 0: K[i][w] = 0\n            elif wt[i-1] <= w: K[i][w] = max(val[i-1] + K[i-1][w-wt[i-1]], K[i-1][w])\n            else: K[i][w] = K[i-1][w]\n    return K[n][W]`,
    Javascript: `function knapSack(W, wt, val, n) {\n    let K = Array(n + 1).fill().map(() => Array(W + 1).fill(0));\n    for (let i = 0; i <= n; i++) {\n        for (let w = 0; w <= W; w++) {\n            if (i === 0 || w === 0) K[i][w] = 0;\n            else if (wt[i - 1] <= w) K[i][w] = Math.max(val[i - 1] + K[i - 1][w - wt[i - 1]], K[i - 1][w]);\n            else K[i][w] = K[i - 1][w];\n        }\n    }\n    return K[n][W];\n}`
  },
  'Floyd-Warshall': {
    C: `void floydWarshall(int graph[][V]) {\n    int dist[V][V], i, j, k;\n    for (i = 0; i < V; i++)\n        for (j = 0; j < V; j++)\n            dist[i][j] = graph[i][j];\n    for (k = 0; k < V; k++)\n        for (i = 0; i < V; i++)\n            for (j = 0; j < V; j++)\n                if (dist[i][k] + dist[k][j] < dist[i][j])\n                    dist[i][j] = dist[i][k] + dist[k][j];\n}`,
    'C++': `void floydWarshall(vector<vector<int>>& graph) {\n    int V = graph.size();\n    vector<vector<int>> dist = graph;\n    for (int k = 0; k < V; k++)\n        for (int i = 0; i < V; i++)\n            for (int j = 0; j < V; j++)\n                if (dist[i][k] + dist[k][j] < dist[i][j])\n                    dist[i][j] = dist[i][k] + dist[k][j];\n}`,
    Java: `void floydWarshall(int graph[][], int V) {\n    int dist[][] = new int[V][V];\n    for (int i = 0; i < V; i++)\n        for (int j = 0; j < V; j++)\n            dist[i][j] = graph[i][j];\n    for (int k = 0; k < V; k++)\n        for (int i = 0; i < V; i++)\n            for (int j = 0; j < V; j++)\n                if (dist[i][k] + dist[k][j] < dist[i][j])\n                    dist[i][j] = dist[i][k] + dist[k][j];\n}`,
    Python: `def floydWarshall(graph, V):\n    dist = list(map(lambda i: list(map(lambda j: j, i)), graph))\n    for k in range(V):\n        for i in range(V):\n            for j in range(V):\n                dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j])\n    return dist`,
    Javascript: `function floydWarshall(graph, V) {\n    let dist = Array.from(graph, row => [...row]);\n    for (let k = 0; k < V; k++) {\n        for (let i = 0; i < V; i++) {\n            for (let j = 0; j < V; j++) {\n                if (dist[i][k] + dist[k][j] < dist[i][j])\n                    dist[i][j] = dist[i][k] + dist[k][j];\n            }\n        }\n    }\n    return dist;\n}`
  },
  'Fractional Knapsack': {
    C: `struct Item { int value, weight; };\nint cmp(struct Item a, struct Item b) {\n    double r1 = (double)a.value / a.weight;\n    double r2 = (double)b.value / b.weight;\n    return r1 > r2;\n}`,
    'C++': `struct Item { int value, weight; };\nbool cmp(struct Item a, struct Item b) {\n    double r1 = (double)a.value / a.weight;\n    double r2 = (double)b.value / b.weight;\n    return r1 > r2;\n}\ndouble fractionalKnapsack(int W, struct Item arr[], int n) {\n    sort(arr, arr + n, cmp);\n    double finalvalue = 0.0;\n    for (int i = 0; i < n; i++) {\n        if (arr[i].weight <= W) { W -= arr[i].weight; finalvalue += arr[i].value; }\n        else { finalvalue += arr[i].value * ((double)W / arr[i].weight); break; }\n    }\n    return finalvalue;\n}`,
    Java: `class Item { int value, weight; }\ndouble fractionalKnapsack(int W, Item arr[], int n) {\n    Arrays.sort(arr, new Comparator<Item>() {\n        public int compare(Item a, Item b) {\n            double r1 = (double)a.value / a.weight;\n            double r2 = (double)b.value / b.weight;\n            return Double.compare(r2, r1);\n        }\n    });\n    double finalvalue = 0.0;\n    for (int i = 0; i < n; i++) {\n        if (arr[i].weight <= W) { W -= arr[i].weight; finalvalue += arr[i].value; }\n        else { finalvalue += arr[i].value * ((double)W / arr[i].weight); break; }\n    }\n    return finalvalue;\n}`,
    Python: `class Item:\n    def __init__(self, value, weight):\n        self.value = value\n        self.weight = weight\ndef fractionalKnapsack(W, arr):\n    arr.sort(key=lambda x: (x.value/x.weight), reverse=True)\n    finalvalue = 0.0\n    for item in arr:\n        if item.weight <= W:\n            W -= item.weight\n            finalvalue += item.value\n        else:\n            finalvalue += item.value * W / item.weight\n            break\n    return finalvalue`,
    Javascript: `function fractionalKnapsack(W, arr) {\n    arr.sort((a, b) => (b.value / b.weight) - (a.value / a.weight));\n    let finalvalue = 0.0;\n    for (let item of arr) {\n        if (item.weight <= W) {\n            W -= item.weight;\n            finalvalue += item.value;\n        } else {\n            finalvalue += item.value * (W / item.weight);\n            break;\n        }\n    }\n    return finalvalue;\n}`
  },
  "Prim's MST": {
    C: `void primMST(int graph[V][V]) {\n    int parent[V], key[V];\n    bool mstSet[V];\n    for (int i = 0; i < V; i++) key[i] = INT_MAX, mstSet[i] = false;\n    key[0] = 0; parent[0] = -1;\n    for (int count = 0; count < V - 1; count++) {\n        int u = minKey(key, mstSet);\n        mstSet[u] = true;\n        for (int v = 0; v < V; v++)\n            if (graph[u][v] && mstSet[v] == false && graph[u][v] < key[v])\n                parent[v] = u, key[v] = graph[u][v];\n    }\n}`,
    'C++': `void primMST(vector<vector<int>>& graph) {\n    int V = graph.size();\n    vector<int> parent(V), key(V, INT_MAX);\n    vector<bool> mstSet(V, false);\n    key[0] = 0; parent[0] = -1;\n    for (int count = 0; count < V - 1; count++) {\n        int u = minKey(key, mstSet);\n        mstSet[u] = true;\n        for (int v = 0; v < V; v++)\n            if (graph[u][v] && mstSet[v] == false && graph[u][v] < key[v])\n                parent[v] = u, key[v] = graph[u][v];\n    }\n}`,
    Java: `void primMST(int graph[][], int V) {\n    int parent[] = new int[V], key[] = new int[V];\n    Boolean mstSet[] = new Boolean[V];\n    for (int i = 0; i < V; i++) { key[i] = Integer.MAX_VALUE; mstSet[i] = false; }\n    key[0] = 0; parent[0] = -1;\n    for (int count = 0; count < V - 1; count++) {\n        int u = minKey(key, mstSet, V);\n        mstSet[u] = true;\n        for (int v = 0; v < V; v++)\n            if (graph[u][v] != 0 && mstSet[v] == false && graph[u][v] < key[v]) {\n                parent[v] = u; key[v] = graph[u][v];\n            }\n    }\n}`,
    Python: `def primMST(graph, V):\n    key = [float('inf')] * V\n    parent = [None] * V\n    key[0] = 0\n    mstSet = [False] * V\n    parent[0] = -1\n    for _ in range(V):\n        u = minKey(key, mstSet)\n        mstSet[u] = True\n        for v in range(V):\n            if graph[u][v] > 0 and mstSet[v] == False and key[v] > graph[u][v]:\n                key[v] = graph[u][v]\n                parent[v] = u`,
    Javascript: `function primMST(graph, V) {\n    let parent = new Array(V), key = new Array(V).fill(Infinity);\n    let mstSet = new Array(V).fill(false);\n    key[0] = 0; parent[0] = -1;\n    for (let count = 0; count < V - 1; count++) {\n        let u = minKey(key, mstSet);\n        mstSet[u] = true;\n        for (let v = 0; v < V; v++)\n            if (graph[u][v] && mstSet[v] === false && graph[u][v] < key[v]) {\n                parent[v] = u; key[v] = graph[u][v];\n            }\n    }\n}`
  },
  "Kruskal's MST": {
    C: `int find(struct subset subsets[], int i) { /* Disjoint set find */ }\nvoid Union(struct subset subsets[], int x, int y) { /* Disjoint set union */ }\n// Sort edges and add to MST if no cycle is formed`,
    'C++': `int find(vector<int>& parent, int i) { if (parent[i] == i) return i; return find(parent, parent[i]); }\n// Kruskal's MST uses union-find and sorts edges by weight.`,
    Java: `// Java implementation of Kruskal's MST.\n// Requires Edge class, sorting array of edges, and Union-Find logic.`,
    Python: `def find(parent, i): return i if parent[i] == i else find(parent, parent[i])\ndef union(parent, rank, x, y):\n    # Union by rank logic\n    pass\n# Sort edges and add to MST avoiding cycles`,
    Javascript: `// Javascript implementation of Kruskal's MST.\n// Uses disjoint set data structure to detect cycles.`
  },
  'N-Queens': {
    C: `bool isSafe(int board[N][N], int row, int col) { /* Check safe */ }\nbool solveNQUtil(int board[N][N], int col) {\n    if (col >= N) return true;\n    for (int i = 0; i < N; i++) {\n        if (isSafe(board, i, col)) {\n            board[i][col] = 1;\n            if (solveNQUtil(board, col + 1)) return true;\n            board[i][col] = 0;\n        }\n    }\n    return false;\n}`,
    'C++': `bool isSafe(vector<vector<int>>& board, int row, int col) { /* Check safe */ }\nbool solveNQUtil(vector<vector<int>>& board, int col) {\n    if (col >= board.size()) return true;\n    for (int i = 0; i < board.size(); i++) {\n        if (isSafe(board, i, col)) {\n            board[i][col] = 1;\n            if (solveNQUtil(board, col + 1)) return true;\n            board[i][col] = 0;\n        }\n    }\n    return false;\n}`,
    Java: `boolean isSafe(int board[][], int row, int col) { /* Check safe */ }\nboolean solveNQUtil(int board[][], int col, int N) {\n    if (col >= N) return true;\n    for (int i = 0; i < N; i++) {\n        if (isSafe(board, i, col)) {\n            board[i][col] = 1;\n            if (solveNQUtil(board, col + 1, N)) return true;\n            board[i][col] = 0;\n        }\n    }\n    return false;\n}`,
    Python: `def solveNQUtil(board, col, N):\n    if col >= N: return True\n    for i in range(N):\n        if isSafe(board, i, col):\n            board[i][col] = 1\n            if solveNQUtil(board, col + 1, N): return True\n            board[i][col] = 0\n    return False`,
    Javascript: `function solveNQUtil(board, col, N) {\n    if (col >= N) return true;\n    for (let i = 0; i < N; i++) {\n        if (isSafe(board, i, col)) {\n            board[i][col] = 1;\n            if (solveNQUtil(board, col + 1, N)) return true;\n            board[i][col] = 0;\n        }\n    }\n    return false;\n}`
  }
};
